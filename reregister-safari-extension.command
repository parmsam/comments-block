#!/bin/bash
# Double-click this file in Finder to re-register the Comments Block Safari extension.
# If macOS blocks it, run once in Terminal: xattr -d com.apple.quarantine reregister-safari-extension.command

cd "$(dirname "$0")"
./reregister-safari-extension.sh
